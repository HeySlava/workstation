---
- name: Install packages using snap (strict)
  community.general.snap:
    name:
      - telegram-desktop
      - firefox
  become: true

- name: Install packages using snap (classic)
  community.general.snap:
    name:
      - code
    classic: true
  become: true
